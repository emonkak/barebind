{
  "name": "kitchen-sink",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "watch": "tsc --build --watch --preserveWatchOutput",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@emonkak/ebit": "workspace:*"
  }
}
