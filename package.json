{
  "name": "barebind",
  "version": "0.1.0",
  "exports": {
    ".": {
      "import": "./src/index.ts"
    },
    "./extras/*": {
      "import": "./src/extras/*.ts"
    },
    "./internal": {
      "import": "./src/internal.js"
    }
  },
  "publishConfig": {
    "exports": {
      ".": {
        "import": "./lib/index.js",
        "types": "./lib/index.d.ts"
      },
      "./extras/*": {
        "import": "./lib/extras/*.js",
        "types": "./lib/extras/*.d.ts"
      },
      "./internal": {
        "import": "./lib/internal.js",
        "types": "./lib/core.d.ts"
      }
    }
  },
  "type": "module",
  "author": "Shota Nozaki",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build",
    "build:all": "pnpm run build && pnpm run --recursive --parallel build",
    "clean": "rm -fr coverage lib",
    "clean:all": "pnpm run --include-workspace-root --recursive --parallel clean",
    "lint": "biome ci",
    "ci": "pnpm run clean:all && pnpm run build:all && pnpm run lint && pnpm test --coverage",
    "test": "vitest run",
    "watch": "tsc --build --watch --preserveWatchOutput",
    "watch:all": "pnpm run --include-workspace-root --recursive --parallel watch"
  },
  "dependencies": {
    "navigation-api-types": "^0.6.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@vitest/browser": "^3.2.4",
    "@vitest/coverage-v8": "3.2.4",
    "playwright": "^1.54.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "sideEffects": false
}
